# mouse-position-tracker-js
A simple JavaScript project that tracks the mouse position inside an element using getBoundingClientRect(), maps the mouse values using mapRange, and creates smooth animations with GSAP.
